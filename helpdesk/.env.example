# Copy to .env and fill in — never commit .env itself.
# Local dev example:
#   DATABASE_URL="postgresql://helpdesk:helpdesk123@localhost:5432/helpdesk?schema=public"
#   DIRECT_URL="postgresql://helpdesk:helpdesk123@localhost:5432/helpdesk?schema=public"

# Local docker-compose DB password (never commit the real value — local .env only)
POSTGRES_PASSWORD=""

# Pooled connection (Neon pooled :6543 URL on Vercel, direct/local URL in dev)
DATABASE_URL=""
# Direct connection for migrations (Neon direct :5432 URL on Vercel, same as DATABASE_URL locally)
DIRECT_URL=""

# Vercel Blob (prod file storage). Local dev falls back to helpdesk/uploads/ disk.
BLOB_READ_WRITE_TOKEN=""

# Seed passwords — required only when running `prisma db seed`, never on Vercel build
SEED_ADMIN_PASSWORD=""
SEED_TECH_PASSWORD=""
SEED_USER_PASSWORD=""
